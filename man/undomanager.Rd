% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/undomanager.R
\name{undomanager}
\alias{undomanager}
\title{Create an undo manager}
\usage{
undomanager(type = NULL, allow_null = FALSE, max_size = Inf)
}
\arguments{
\item{type}{The permitted classes of the items (\code{NULL} to allow any
object). An item is accepted when any of these classes appears among
the classes R would dispatch on, so \code{"numeric"} also accepts integers
and numeric matrices.}

\item{allow_null}{Whether \code{NULL} values are allowed. Only used when
\code{type} is given; an untyped manager accepts any object, including \code{NULL}.}

\item{max_size}{The maximum number of items to keep. Once the history
grows past this, the oldest items are dropped. Use \code{Inf} for no limit.}
}
\value{
A new \link{UndoManager} object.
}
\description{
Create a new \link{UndoManager} to track the history of an object and move
through it with undo and redo operations. The manager starts out empty; the
first call to \verb{$do()} gives it a value.
}
\details{
This is the recommended way to create a manager. It is equivalent to
\code{UndoManager$new()}, which is also available for anyone who wants to work
with the \link[R6:R6Class]{R6} class directly, such as to subclass it.

A manager is an R6 object, so it has reference semantics: its methods modify
the manager in place instead of returning a modified copy, and assigning a
manager to a second variable does not copy it.
}
\examples{
nums <- undomanager()
nums$do(5)
nums$do(7)
nums$do(10)
nums$undo()$value
nums$redo()$value

# operations return the manager, so they can be chained
undomanager()$do(1)$do(2)$do(3)$undo(2)$value

undomanager(type = "numeric")
undomanager(type = "numeric", allow_null = TRUE)
undomanager(max_size = 10)
}
\seealso{
\link{UndoManager} for the full list of methods and active bindings.
}
