% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/undomanager.R
\name{UndoManager}
\alias{UndoManager}
\title{Undo/Redo manager}
\description{
With the undo manager, you can manage the history of an object by
using undo and redo operations.

Managers are usually created with \code{\link[=undomanager]{undomanager()}} rather than by calling
\code{UndoManager$new()} directly. The class itself is exported for anyone who
wants to work with it directly, such as to subclass it.
}
\examples{
nums <- undomanager()
nums$do(5)
nums$do(7)
nums$do(10)
nums$undo()$value
nums$redo()$value

# operations return the manager, so they can be chained
undomanager()$do(1)$do(2)$do(3)$undo(2)$value

## ------------------------------------------------
## Method `UndoManager$new()`
## ------------------------------------------------

UndoManager$new()
UndoManager$new(type = "numeric")
UndoManager$new(type = "numeric", allow_null = TRUE)
UndoManager$new(max_size = 10)

## ------------------------------------------------
## Method `UndoManager$reactive()`
## ------------------------------------------------

if (requireNamespace("shiny", quietly = TRUE)) {
  nums <- undomanager()$do(5)
  rx <- nums$reactive()
  shiny::isolate(rx()$value)
}

## ------------------------------------------------
## Method `UndoManager$print()`
## ------------------------------------------------

undomanager()$do(5)$do(7)$do(10)$undo()$print()

## ------------------------------------------------
## Method `UndoManager$undo()`
## ------------------------------------------------

nums <- undomanager()$do(5)$do(7)$do(10)
nums$undo()$value
nums$undo(Inf)$value

## ------------------------------------------------
## Method `UndoManager$redo()`
## ------------------------------------------------

nums <- undomanager()$do(5)$do(7)$do(10)$undo(2)
nums$redo()$value
nums$redo(Inf)$value

## ------------------------------------------------
## Method `UndoManager$do()`
## ------------------------------------------------

nums <- undomanager()
nums$do(5)
nums$do(7)$value
nums$undo()$do(99)$redo_size

## ------------------------------------------------
## Method `UndoManager$clear()`
## ------------------------------------------------

nums <- undomanager()$do(5)$do(7)
nums$clear()$undo_size
nums$value
nums$clear(clear_value = TRUE)$is_empty
}
\seealso{
\code{\link[=undomanager]{undomanager()}}, the recommended way to create a manager.
}
\section{Active bindings}{
  \if{html}{\out{<div class="r6-active-bindings">}}
  \describe{
    \item{\code{value}}{Get the value}

    \item{\code{is_empty}}{Whether the manager holds no value at all}

    \item{\code{can_undo}}{Whether there are any undo operations available}

    \item{\code{can_redo}}{Whether there are any redo operations available}

    \item{\code{undo_size}}{Get the number of undo operations}

    \item{\code{redo_size}}{Get the number of redo operations}
  }
  \if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
  \itemize{
    \item \href{#method-UndoManager-initialize}{\code{UndoManager$new()}}
    \item \href{#method-UndoManager-reactive}{\code{UndoManager$reactive()}}
    \item \href{#method-UndoManager-print}{\code{UndoManager$print()}}
    \item \href{#method-UndoManager-undo}{\code{UndoManager$undo()}}
    \item \href{#method-UndoManager-redo}{\code{UndoManager$redo()}}
    \item \href{#method-UndoManager-do}{\code{UndoManager$do()}}
    \item \href{#method-UndoManager-clear}{\code{UndoManager$clear()}}
  }
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-UndoManager-initialize"></a>}}
\if{latex}{\out{\hypertarget{method-UndoManager-initialize}{}}}
\subsection{\code{UndoManager$new()}}{
  Create a new undo manager. The manager starts out empty; the first
call to \code{do()} gives it a value. Most users should call
\code{\link[=undomanager]{undomanager()}} instead, which is equivalent.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{UndoManager$new(type = NULL, allow_null = FALSE, max_size = Inf)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{type}}{The permitted classes of the items (\code{NULL} to allow any
object). An item is accepted when any of these classes appears among
the classes R would dispatch on, so \code{"numeric"} also accepts integers
and numeric matrices.}
      \item{\code{allow_null}}{Whether \code{NULL} values are allowed. Only used when
\code{type} is given; an untyped manager accepts any object, including \code{NULL}.}
      \item{\code{max_size}}{The maximum number of items to keep. Once the history
grows past this, the oldest items are dropped. Use \code{Inf} for no limit.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A new \code{UndoManager} object.
  }
  \subsection{Examples}{
    \if{html}{\out{<div class="r example copy">}}
    \preformatted{UndoManager$new()
UndoManager$new(type = "numeric")
UndoManager$new(type = "numeric", allow_null = TRUE)
UndoManager$new(max_size = 10)
}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-UndoManager-reactive"></a>}}
\if{latex}{\out{\hypertarget{method-UndoManager-reactive}{}}}
\subsection{\code{UndoManager$reactive()}}{
  Get a shiny reactive for this manager, so that it can be used inside a
reactive context. The returned reactive invalidates whenever the value
changes, and calling it returns the manager itself.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{UndoManager$reactive()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A shiny reactive expression that returns the manager.
  }
  \subsection{Examples}{
    \if{html}{\out{<div class="r example copy">}}
    \preformatted{if (requireNamespace("shiny", quietly = TRUE)) {
  nums <- undomanager()$do(5)
  rx <- nums$reactive()
  shiny::isolate(rx()$value)
}
}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-UndoManager-print"></a>}}
\if{latex}{\out{\hypertarget{method-UndoManager-print}{}}}
\subsection{\code{UndoManager$print()}}{
  Print the manager: the classes it accepts, how many undo and redo
operations are available, the current item, and the items in the undo
and redo history.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{UndoManager$print(...)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{...}}{Not used.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    The manager, invisibly.
  }
  \subsection{Examples}{
    \if{html}{\out{<div class="r example copy">}}
    \preformatted{undomanager()$do(5)$do(7)$do(10)$undo()$print()
}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-UndoManager-undo"></a>}}
\if{latex}{\out{\hypertarget{method-UndoManager-undo}{}}}
\subsection{\code{UndoManager$undo()}}{
  Move back in the history, making the previous item current. Undoing
when there is nothing left to undo does nothing.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{UndoManager$undo(n = 1)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{n}}{The number of undo operations to perform. If \code{n} is larger
than the number of available undo operations, all of them are
performed. Use \code{Inf} to undo the entire history.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    The manager, invisibly.
  }
  \subsection{Examples}{
    \if{html}{\out{<div class="r example copy">}}
    \preformatted{nums <- undomanager()$do(5)$do(7)$do(10)
nums$undo()$value
nums$undo(Inf)$value
}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-UndoManager-redo"></a>}}
\if{latex}{\out{\hypertarget{method-UndoManager-redo}{}}}
\subsection{\code{UndoManager$redo()}}{
  Move forward in the history, reversing an undo. Any redo history is
discarded as soon as a new item is added with \code{do()}. Redoing when
there is nothing left to redo does nothing.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{UndoManager$redo(n = 1)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{n}}{The number of redo operations to perform. If \code{n} is larger
than the number of available redo operations, all of them are
performed. Use \code{Inf} to redo the entire history.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    The manager, invisibly.
  }
  \subsection{Examples}{
    \if{html}{\out{<div class="r example copy">}}
    \preformatted{nums <- undomanager()$do(5)$do(7)$do(10)$undo(2)
nums$redo()$value
nums$redo(Inf)$value
}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-UndoManager-do"></a>}}
\if{latex}{\out{\hypertarget{method-UndoManager-do}{}}}
\subsection{\code{UndoManager$do()}}{
  Add an item and make it the current value. The item that was current
becomes the most recent undo, and any redo history is discarded.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{UndoManager$do(item)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{item}}{The item to add. It must satisfy the manager's \code{type}, if
one was given.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    The manager, invisibly.
  }
  \subsection{Examples}{
    \if{html}{\out{<div class="r example copy">}}
    \preformatted{nums <- undomanager()
nums$do(5)
nums$do(7)$value
nums$undo()$do(99)$redo_size
}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-UndoManager-clear"></a>}}
\if{latex}{\out{\hypertarget{method-UndoManager-clear}{}}}
\subsection{\code{UndoManager$clear()}}{
  Forget the undo and redo history, keeping the current value.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{UndoManager$clear(clear_value = FALSE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{clear_value}}{Whether to also discard the current value, leaving
the manager empty.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    The manager, invisibly.
  }
  \subsection{Examples}{
    \if{html}{\out{<div class="r example copy">}}
    \preformatted{nums <- undomanager()$do(5)$do(7)
nums$clear()$undo_size
nums$value
nums$clear(clear_value = TRUE)$is_empty
}
    \if{html}{\out{</div>}}
  }
}

}
