% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/undomanager.R
\name{all.equal.UndoManager}
\alias{all.equal.UndoManager}
\title{Compare two UndoManager objects}
\usage{
\method{all.equal}{UndoManager}(target, current, ...)
}
\arguments{
\item{target, current}{The two \code{UndoManager} objects to compare.}

\item{...}{Passed on to \code{\link[=all.equal]{all.equal()}}.}
}
\value{
\code{TRUE} if the two managers are equal, otherwise a character vector
describing the differences.
}
\description{
Two managers are equal when they hold the same value, the same undo and redo
history, and the same \code{type} restriction. Internal bookkeeping, such as the
counter used to trigger shiny reactivity, is ignored, so two managers that
reached the same state by different routes compare as equal.
}
\details{
Note that \code{\link[=identical]{identical()}} cannot be used to compare managers: R6 objects are
environments, so \code{identical()} always reports two separate managers as
different regardless of their contents.
}
